import 'package:flutter/material.dart';

class CurrencyChart extends StatelessWidget {

  final firstWeek;
  final secondWeek;
  final thirdWeek;
  final fourthWeek;
  final currentWeek;

  CurrencyChart({this.firstWeek, this.secondWeek, this.thirdWeek, this.fourthWeek, this.currentWeek});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
